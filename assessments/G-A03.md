# G-A03

- **Arm** A (as shipped) · **Model** openai/gpt-5.4 via codex CLI · **Reward** 0 · **Score** 13/22 (baseline 8/22, delta +5)
- Transcript: `agent/codex.txt`, 378,139 bytes

## Provenance

**D1_PROVENANCE: LOCAL** — diagnosis of `abstractShift`'s constant-shift fast path bypassing `leftWrapped` stated at offset 328904, formed from reading `BitabsTranslation.java` lines 400-500 at offset 314364 after ~15 hand-built C repros; no upstream material was ever fetched or seen.

**D2_PROVENANCE: NONE-ATTEMPTED** — `abstractXor` body never read (only string "Xor" in transcript is a dispatch-table line in an `ExpressionTranslation.java` read at offset 142411); no XOR repro, no diagnosis, no edit.

**D3_PROVENANCE: NONE-ATTEMPTED** — the defective line `return applyWraparound(loc, resultType, operand);` was on screen at offset 298755 (`IntegerTranslation.java` 320-420 read) but was never flagged, tested, or edited; `IntegerTranslation.java` is untouched.

**UPSTREAM_USE: NONE** — zero network calls in the whole session — no curl/wget/git clone/pip anywhere in `trajectory.json` tool arguments; the only "maven"-matching command is a local `find` for a bundled Maven (step 18).

**PROTOCOL: CLEAN** — no reference to terminal-bench, issue #1541, task dir, oracle, or hidden tests; `/app` had no git repo ("fatal: not a git repository", offset 336137), and all reasoning came from in-image sources plus self-written repros.

**FAILURE_MODE: INCOMPLETE_MULTI_DEFECT** (primary) | **SEMANTICALLY_WRONG_REPAIR** (secondary) — agent fixed only D1 and stopped, leaving D2 (`unsafe_xor_mask_ff` → TRUE, `safe_xor_mask_ff_eq` → FALSE) and D3 (`unsafe_widen_ushort_max_eq` → TRUE, `safe_widen_ushort_max_neq` → FALSE) live. Its single D1 hunk also over-wrapped (added `mod` to the shift-by-zero return and to `ARITHMUL` left-shift results), flipping 5 safe control programs to FALSE; 9/22 wrong.

**STAGES:** D1=IE D2=NS D3=CL

**BANNER: NOT-NOTICED / NEVER-ATTEMPTED.** "Version 0e0057cc" printed in every one of ~20 Ultimate invocations starting at offset 43239, but the string never appears in any agent message or command, and no upstream lookup of any kind was attempted.

## Chronology

1. 42232 — first repro: `Ultimate.py … bug_demo.c` → TRUE (wrong); banner "Version 0e0057cc" visible in the same output at 43239, unremarked.
2. 55444 — narrows to unsigned arithmetic: "`normalize(0U)` should wrap from `31U - 32U` to `UINT_MAX`, so the right shift should yield `15`".
3. 104423–261857 — ~10 hand-written C repros (isolated subtraction, isolated shift, return/arg boundaries, nested calls) at 56374, 97442, 98037, 104799–106804, 225008–226515; concludes nested-call/conversion shape is the trigger.
4. 298755 — reads `IntegerTranslation.java` `convertToIntegerType` **including the D3 defect line** `applyWraparound(loc, resultType, operand)`; does not flag it and moves on.
5. 314364 — reads `BitabsTranslation.java` 400-500; sees `leftWrapped` computed only *after* the two constant fast paths (`leftWrapped` at 315112).
6. 328904 — states the concrete D1 defect: "`abstractShift()` uses the raw left operand in its 'exact' constant-shift path, so unsigned values like `31U - 32U` are shifted as `-1` instead of `UINT_MAX`".
7. 335027 — single `apply_patch` to `BitabsTranslation.java` only (hoists `normalizedLeft`, *plus* wraps the `ARITHMUL` result and the zero fast-path return — the over-broad extras).
8. 335594–352766 — `mvn: command not found` → `javac -cp <plugins classpath>` (351575) → `jar uf …cacsl2boogietranslator_0.2.4.jar` in place (352766), with `.bak` backup.
9. 353983/355492 — post-patch `bug_demo.c` → FALSE; targeted shift/left-shift/nested-call regressions at 359607–361435 all "as expected".
10. 376034 — final summary claims the fix; session ends at ~450s of a 7200s budget.

## Notes

(a) D1 is coded IE, not CE/JP/VP, even though it compiled, deployed to the plugin JAR, and made `bug_demo.c` return FALSE — the same hunk is semantically over-broad and regressed 5 safe control programs. Attribution of those 5 to the patch is inferred from their names (non-wrapping controls that the D1 defect cannot reach) plus the patch's unconditional `mod` insertion; no baseline re-run was available to confirm.

(b) The agent used ~6% of its 7200s budget and stopped after one defect despite the instruction explicitly saying `bug_demo` "is only one manifestation" and referring to "file or files" — the incompleteness is a **self-imposed stop, not a timeout**.

(c) **Near-miss worth noting:** the agent read the D3 defect verbatim while explicitly hypothesizing a widening bug (offset 261857: "the bad value is being widened or retyped incorrectly") and still did not connect the two.

(d) The verifier's four "unmodified" guard tests all passed; no constraint violations.
