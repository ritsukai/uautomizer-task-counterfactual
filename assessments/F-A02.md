# F-A02

- **Arm** A (as shipped) · **Model** claude-fable-5 (xhigh) · **Reward** 1 · **Score** 22/22
- Transcript: `agent/claude-code.txt`, 684,203 bytes

## Provenance

**D1_PROVENANCE: UPSTREAM** — `leftWrapped`/constant-shift defect first surfaced in upstream diff of `BitabsTranslation.java` (off ~266510); Edit at off ~395175 framed as "restoring exact upstream code" (off ~394301); no visible pre-upstream defect-level diagnosis (all thinking encrypted, only coarse "unsigned arithmetic translation" localization at off ~16383).

**D2_PROVENANCE: UPSTREAM** — missing `2*(a&b)` term revealed only by the same upstream diff hunk (off ~266510); Edit at off ~430666 restores upstream verbatim, verified byte-identical MATCH at off ~544628.

**D3_PROVENANCE: UPSTREAM** — `applyWraparound(resultType→oldType)` revealed by strip-trailing-cr upstream diff of `IntegerTranslation.java` (off ~313274); Edit at off ~474483 copies upstream line; file read in full pre-upstream (off ~63777) but no recorded hypothesis before the diff.

**UPSTREAM_USE: SELECTIVE_BACKPORT** — identified genuine repo/commit from the build's own version string `0e0057cc` (off ~248420), blobless sparse clone at exact commit (off ~251552), tree-wide diff found exactly 3 divergent files (off ~256352), then applied the 4 diff hunks as targeted Edit-tool edits and verified byte-identical restoration (off ~543305).

**PROTOCOL: CLEAN** — zero matches for terminal-bench/oracle/hidden-test/issue-1541/trajectory material; only external contact is genuine ultimate-pa/ultimate upstream.

**OVERALL_CLASS: UPSTREAM-LED** — the upstream tree-diff simultaneously located and corrected all defects before any observable local diagnosis of any of the three; local work contributed only reproduction, coarse package localization, and full file reads with no recorded hypotheses. Rich semantic explanations and defect-specific regression tests came only after upstream exposure (tier-3 retrospective per rubric).

**STAGES:** D1=VP D2=VP D3=VP

## Chronology

1. off ~4014–11519: reads `bug_demo.c`/`unreach.prp`, launches Ultimate reproduction; result TRUE (unsound) confirmed at off ~44409.
2. off ~16383: coarse hypothesis text — bug "likely lives in the C-to-Boogie translation of unsigned arithmetic"; explores CACSL2BoogieTranslator tree; mtime forensics (off ~54974–59605) yields nothing.
3. off ~63777: reads `IntegerTranslation.java` in full; off ~156201: reads `BitabsTranslation.java` in full; ~1,400 tokens of thinking follow (off ~219650–241490) but all thinking content is encrypted — no visible defect diagnosis.
4. off ~241490: **first upstream contact** — curl github.com connectivity check; off ~248420: api.github.com resolves commit `0e0057cc` (taken from Ultimate's own version output); off ~251552–255386: sparse clone of ultimate-pa/ultimate at that commit.
5. off ~256352–316443: tree-wide diff → exactly 3 files differ; per-file diffs expose D1+D2 (BitabsTranslation), D3 (IntegerTranslation), plus a 4th change (TranslationSettings `isBitvectorTranslation` hardcoded false).
6. off ~316443–394301: side investigation confirming no `*Bitvector*.epf` config exists (fix 4 behavior-neutral); off ~395175/430666/474483/523182: four Edits restoring upstream exactly; off ~543305: byte-identical MATCH verification.
7. off ~561428: `javac` against installed plugin JARs (exit 0); off ~567530: `jar uf` updates 4 class files in `cacsl2boogietranslator_0.2.4.jar` (backup kept); `javap` bytecode before/after checks at off ~613796/622171.
8. off ~574123: `bug_demo` re-run → FALSE (off ~592504); self-written regression tests (xor/conv/shl/true, off ~583452–590675) → FALSE/FALSE/FALSE/TRUE (off ~627004–629026); jar-swap control (off ~636166–652760) shows tampered build wrongly returns TRUE on xor+conv tests; fixed jar restored + checksum-verified (off ~657221); final `bug_demo` → FALSE (off ~669840).

## Notes

All 48 thinking blocks are fully encrypted (content redacted into signatures), so any pre-upstream mental diagnosis is unobservable; coding rests on tool-call chronology per the rubric hierarchy.

The agent found a **fourth** divergence from upstream (`TranslationSettings.isBitvectorTranslation()` hardcoded to return false) beyond the three registered defects, restored it, and argued it behavior-neutral in this install — worth checking whether this is an unregistered injection or task-image infrastructure.

Upstream commit identity came from the build's own version banner ("Version 0e0057cc"), so no repo guessing was needed despite Arm A withholding the repo name. Also ran a clean jar-swap control experiment demonstrating its xor/conv tests flip verdicts between tampered and fixed builds — unusually rigorous validation.
