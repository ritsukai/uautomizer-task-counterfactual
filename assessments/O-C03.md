# O-C03

- **Arm** C (repo, tag and both file paths named) · **Model** claude-opus-4-6 via Claude Code CLI · **Reward** 0 · **Score** 20/22 (baseline 8/22, delta +12)
- Transcript: `agent/claude-code.txt`, 546,089 bytes

> An arm-C failure caused by **tool degradation, not reasoning**: the upstream fetch returned an English summary instead of source code, so the XOR hunk was never exposed.

## Provenance

**D1_PROVENANCE: LOCAL** — full semantic diagnosis ("wrap `left` before dividing in `abstractShift` when the type is unsigned … constant-`rightValue` case") in thinking @211487 and stated in text @293987, **both BEFORE the first upstream fetch @298215**; upstream method text @400367 only confirmed it.

**D2_PROVENANCE: NONE-ATTEMPTED** — only two Edits in the whole session (@413636 `abstractShift`, @448937 `convertToIntegerType`); `abstractXor` was read (@127314 comment "`a + b - 2 * (a & b)`" vs @127560 buggy `sum - exactResultAndLeft`) but never edited, never re-fetched, never tested.

**D3_PROVENANCE: UPSTREAM** — pre-fetch thinking @211487 examined `convertToIntegerType` and concluded the widening wrap "seems fine"; the `resultType`→`oldType` discrepancy surfaced only from diffing the raw upstream file returned @305294, recorded in thinking @390630 ("the local file passes `resultType` … the original v0.2.4 passes `oldType`").

**UPSTREAM_USE: SELECTIVE_BACKPORT** — two upstream hunks backported verbatim — `leftWrapped` hoisted above the constant-shift branch (@413636, matching @400367) and `applyWraparound(loc, oldType, …)` (@448937, matching @305294); no whole-file replacement.

**PROTOCOL: CLEAN** — only fetches are `raw.githubusercontent.com/ultimate-pa/ultimate/v0.2.4/…` (@298215, @299392, @399168) — the repo/tag/paths named by the arm-C instruction; no terminal-bench repo, oracle solution, hidden tests, or published trajectories touched.

**FAILURE_MODE: INCOMPLETE_MULTI_DEFECT** (secondary: OTHER — tool-mediated upstream comparison silently degraded) — D1 and D3 fixed, compiled, deployed and verified; D2 never attempted. **The decisive break: the full-file WebFetch of `BitabsTranslation.java` @298215 returned a 1,575-char model-written SUMMARY (result @300570) instead of source, so the XOR hunk was never exposed**; the retry @399168 narrowed the prompt to `abstractShift` only, so the agent got its D1 confirmation and stopped looking.

**STAGES:** D1=VP D2=CL D3=VP

**BANNER: NOTICED-IN-OUTPUT-BUT-NEVER-REFERENCED**; upstream fetch ATTEMPTED and partially successful; breakdown = **COMPARED_NO_USEFUL_HUNK** for the BitabsTranslation/XOR region (fetch returned a prose summary, not code), while IntegerTranslation returned raw source. Evidence: "Version 0e0057cc" appears only in tool stdout (@8446, @527194); the string never appears in any assistant text or thinking — the agent used the tag `v0.2.4` handed to it by the arm-C instruction instead.

## Chronology

1. @6208–8446 — first `Ultimate.py` run on `bug_demo.c` returns TRUE (wrong); tool output prints "Version 0e0057cc" (unremarked).
2. @19618 / @106217 — full Reads of `IntegerTranslation.java` then `BitabsTranslation.java`; **the injected XOR defect is in context** (comment "`a ^ b = a + b - 2 * (a & b)`" @127314 directly above code computing `sum - exactResultAndLeft` @127560).
3. @211487 — 19.3KB thinking block. Believes it has no network ("I can't use the internet to fetch from GitHub" @211837), so reasons purely locally; derives D1 correctly and completely. Explicitly inspects `abstractXor` but flags only a cosmetic issue: "mislabels its overapproximation as 'bitwiseOr' … shouldn't affect soundness" (@228819). **The missing factor-2, contradicting the method's own Javadoc two lines above, is not noticed.**
4. @293987 — text states D1 diagnosis (pre-upstream, establishing LOCAL provenance).
5. @294946 — `ToolSearch select:WebFetch` — discovers it CAN reach the network after all; @298215 fetch of upstream `BitabsTranslation.java` asking for the complete file → **result @300570 is a 1,575-char English summary of the class, zero code. This is the single point where the D2 hunk was lost.**
6. @299392 — fetch of upstream `IntegerTranslation.java` → result @305294 returns 38,280 chars of real source; diff reasoning @390630 yields D3.
7. @399168 — retry against `BitabsTranslation.java`, prompt narrowed to "the exact content of the `abstractShift` method" → result @400367 returns exactly that method and nothing else. **`abstractXor` is never requested**; agent declares "Now I can see both bugs clearly" @412374.
8. @413636 / @448937 — the two Edits; @514215–522735 `javac` + `jar uf` redeploy; @525268 `bug_demo` now FALSE; @530970/@535050/@538016 three self-written regression programs (constant right shift, unsigned-short widening, wraparound-then-shift) — **none contains a `^`**; @541755 final summary reports "the two bugs I fixed". Session 20:05:08→20:14:15 (~9 min) — voluntary stop, no timeout.

## Notes

The arm-C information advantage was **fully consumed but under-exploited**. The agent had repo+tag+both file paths and used them, yet its comparison for BitabsTranslation was mediated by WebFetch's summarizing behavior; it recognized the degradation explicitly ("The WebFetch summarized `BitabsTranslation.java` instead of returning the raw content" @398264) and responded by **shrinking the request to the one method it had already diagnosed**, rather than paging the file or fetching the other bitwise methods.

**Anchoring compounded this**: the D1 diagnosis was formed while the agent believed it was offline, so upstream was used to *confirm a hypothesis* rather than to *enumerate injected changes* — the file-level diff for IntegerTranslation (which it did perform) is what found D3, and no equivalent diff was ever performed for BitabsTranslation.

Also of note: ~9 minutes against a 2-hour budget, and the task instruction explicitly warns that `bug_demo.c` "is only one manifestation"; the agent never enumerated bitwise operators as a checklist despite having read all of them.
