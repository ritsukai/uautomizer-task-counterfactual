# O-B01

- **Arm** B (generic upstream-comparison sentence, no coordinates) · **Model** claude-opus-4-6 via Claude Code CLI · **Reward** 0 · **Score** 12/22 (baseline 8/22, delta +4)
- Transcript: `agent/claude-code.txt`, 352,196 bytes

> **Arm B's added sentence produced no observable behaviour change of any kind in this trial.**

## Provenance

**D1_PROVENANCE: LOCAL** — subagent's from-scratch Nutz-transformation diagnosis ("division does NOT preserve modular equivalence", byte 232116) precedes the edit at 266883; zero upstream material existed in the session.

**D2_PROVENANCE: NONE-ATTEMPTED** — `abstractXor`'s `a + b - (a&b)` shortcut was displayed inside the whole-file Read at byte 189454 but never edited, quoted, or reasoned about.

**D3_PROVENANCE: NONE-ATTEMPTED** — `convertToIntegerType`'s `applyWraparound(loc, resultType, operand)` was displayed at byte ~81000 (`IntegerTranslation.java` full Read) yet never mentioned; only one Edit in the whole session (266883).

**UPSTREAM_USE: NONE** — zero WebFetch/WebSearch tool calls; final usage record `"web_search_requests":0,"web_fetch_requests":0` at byte 350059; strings upstream/baseline/github/git/clone/curl/wget occur **0 times** in the transcript.

**PROTOCOL: CLEAN** — all 30 Bash calls are local (cat/find/grep/javac/jar/Ultimate.py); no network egress, no terminal-bench or oracle material touched.

**FAILURE_MODE: INCOMPLETE_MULTI_DEFECT** (secondary: DEMO_OVERFIT) — one correct, correctly-deployed D1 repair; D2 and D3 never attempted, so 10/22 hidden programs stay wrong. Agent stopped as soon as `bug_demo.c` flipped plus three self-authored shift-only regression tests passed, despite the prompt warning the demo is "only one manifestation."

**STAGES:** D1=VP D2=CL D3=CL

**BANNER: NOT NOTICED / NEVER-ATTEMPTED** — the banner "Version 0e0057cc" entered context in the very first Ultimate run (byte 8391, repeated 9941, 330774, 332329) and the agent never referenced it, never wrote "version"/"commit"/"upstream"/"baseline", and made zero web calls. **The arm-B directive ("consider locating a plausible upstream baseline and comparing it with the local source", `trajectory.json` byte 2431) was silently ignored**; the upstream route broke down at step zero — before any search — so it maps to NONE, not SEARCH_NO_BASELINE.

## Chronology

1. 2460–5637 — Reads `/app/tests/bug_demo.c` and `unreach.prp`.
2. 6161→8203 — Runs Ultimate on the demo → TRUE (wrong). The build banner "Version 0e0057cc" is printed in this same tool result at byte 8391; no reaction.
3. 14392–17412 — Hand-simulates C semantics (`31U−32U → 0xFFFFFFFF`, `>>28 = 15` → must be FALSE) and localizes to the C→Boogie unsigned translation layer.
4. 38668 — Dispatches one subagent, scoped purely to local files under `expressiontranslation/` and `chandler/`; **the prompt contains no upstream/version instruction**.
5. 60154 / 104982 / 169874 — Whole-file Reads of `IntegerTranslation.java` (D3 defect text visible ~81000), `ExpressionTranslation.java`, and `BitabsTranslation.java` (D2 defect text visible at 189454) — **both defects surfaced but unexamined**.
6. 232116 — Subagent returns a fully local D1 diagnosis and **explicitly forecloses further search**: "The `abstractShift` method's constant path is the one place that fails to do so."
7. 266883 — Single Edit adds `final Expression leftWrapped = applyWraparoundIfNecessary(loc, left, typeLeft)` in the `rightValue != null` branch; 325319 `javac -encoding UTF-8` succeeds; 326533 `jar uf` into `...cacsl2boogietranslator_0.2.4.jar`.
8. 330586 — Demo now FALSE; 341114 / 342484 / 347826 three self-written tests (safe shift, unsigned-wrap shift, signed shift) all correct; 348414 final summary describes the fix as a single defect; 350059 session ends after ~405s of API time.

## Notes

The D1 edit is semantically equivalent to the described upstream placement (wraparound hoisted above the constant fast path) and its own regression tests pass, so **the failure is purely arms-length coverage, not a bad repair**.

The subagent's report is the proximate cause of incompleteness: it explicitly declared the constant-shift path "the one place" lacking wraparound, and the main agent accepted that without independent sweep — even though **the D2 shortcut visibly contradicts its own adjacent comment** ("`a ^ b = a + b - 2 * (a & b)`" at line 328 while the code subtracts `(a&b)` once).

D2/D3 stages coded CL on the basis that the defective regions were surfaced by whole-file Reads and `abstractXor` was named by the subagent; a stricter reading (defect-directed inspection required) would give NS for both.
