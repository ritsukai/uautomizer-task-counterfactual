# F-C02

- **Arm** C (repo, tag and both file paths named) · **Model** claude-fable-5 (xhigh) · **Reward** 1 · **Score** 22/22
- Transcript: `agent/claude-code.txt`, 419,574 bytes

## Provenance

**D1_PROVENANCE: UPSTREAM** — first upstream fetch (curl raw.githubusercontent.com v0.2.4, both files) @byte23961 preceded any local read of the shift code; diff @byte27186 exposed the `leftWrapped`/constant-fast-path hunk with the exact correction; edit @byte181332 restores upstream text verbatim; no local hypothesis stated before @byte23961.

**D2_PROVENANCE: UPSTREAM** — same diff @byte27186 exposed the missing "2 *" factor in the XOR shortcut before any local read of `abstractXor`; edit @byte226762 restores upstream text verbatim (the agent's semantic gloss "line 227's block is `abstractOr`... `a+b-(a&b)` is correct" @byte225856 comes after upstream exposure).

**D3_PROVENANCE: UPSTREAM** — upstream diff @byte73603 exposed the single hunk `applyWraparound(loc, resultType, ...)` vs upstream `oldType` before any local read of IntegerTranslation; edit @byte132046 flips `resultType→oldType` exactly per the diff.

**UPSTREAM_USE: SELECTIVE_BACKPORT** — fetched both named files at the named tag @byte23961, diffed @byte26103–@byte84764, then applied three hunk-level Edits (@132046, @181332, @226762) restoring exactly the diff hunks; result verified byte-identical to upstream @byte266527 ("BOTH FILES MATCH UPSTREAM v0.2.4").

**PROTOCOL: CLEAN** — no mention of issue #1541, terminal-bench, oracle solution, hidden tests, or published trajectories anywhere in the transcript; upstream repo/tag/file names came from the arm C instruction itself (part of the assigned condition).

**OVERALL_CLASS: UPSTREAM-LED** — the agent's second substantive move after reproducing was to fetch upstream and diff; all three defect locations **and** their exact corrections came from the diff. All post-diff semantic explanations are retrospective; no defect had a prior local diagnosis.

**STAGES:** D1=VP D2=VP D3=VP

## Chronology

1. @byte4651–@byte15630 — read `bug_demo.c`/`unreach.prp`, ran `Ultimate.py`: reproduced wrong verdict TRUE (expected FALSE); stated @byte20751.
2. @byte21571 — `wc -l` on the two local files only (no content read, no hypothesis formed).
3. @byte23961 — **first upstream contact**: curl `raw.githubusercontent.com/ultimate-pa/ultimate/v0.2.4` for `BitabsTranslation.java` + `IntegerTranslation.java`.
4. @byte26103–@byte84764 — diffs vs upstream: 2 hunks in Bitabs (D1 shift `leftWrapped`, D2 XOR missing 2× factor), 1 hunk in Integer (D3 `resultType` vs `oldType`); "Found three deviations from upstream" @byte82535.
5. @byte87639, @byte121099 — local reads around the hunks (post-upstream-exposure comprehension only).
6. @byte132046 (D3 edit), @byte181332 (D1 edit, after a non-unique-match error @byte175770), @byte226762 (D2 edit); files verified byte-identical to upstream @byte266527.
7. @byte289175 `javac` exit 0; @byte294631 `jar uf` into cacsl2boogietranslator JAR; rerun @byte297121 → FALSE @byte298738.
8. @byte346078–@byte398065 — **JAR-timestamp forensics** found a 4th tampered class (`TranslationSettings.isBitvectorTranslation` hardcoded false), upstream-fetched/restored/compiled/jarred it; 5 self-written directional tests + `bug_demo` all give expected verdicts @byte398065.

## Notes

All thinking blocks are redacted (empty string + signature), so only tool calls and assistant text are codeable — a rich private local diagnosis before @byte23961 cannot be ruled in, but nothing observable supports one and the fetch came ~9KB after reproduction with no intervening file reads.

Transcript reveals a **fourth** injected deviation (`TranslationSettings.java`, `isBitvectorTranslation()` hardcoded to false) beyond the pre-registered D1–D3; agent found it via 2026 vs 2023 JAR-entry timestamps and restored it too. Agent also scanned every plugin JAR for 2026-dated entries to confirm no other tampering (@byte380804).
