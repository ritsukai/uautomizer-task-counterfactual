# F-C03

- **Arm** C (repo, tag and both file paths named) · **Model** claude-fable-5 (xhigh) · **Reward** 1 · **Score** 22/22
- Transcript: `agent/claude-code.txt`, 501,831 bytes

## Provenance

**D1_PROVENANCE: UPSTREAM** — only local read of BitabsTranslation (@22511) with no stated hypothesis before first upstream fetch (@94349); D1 edit (@346935) reproduces the upstream hunk verbatim (`leftWrapped` hoisted before constant fast path), later verified byte-identical to upstream (@384949).

**D2_PROVENANCE: UPSTREAM** — no contemporaneous XOR diagnosis anywhere before the upstream diff was read (@135088); D2 edit (@308784) inserts the exact upstream `2·(a&b)` text; agent's own summary (@494318): "I diffed the two local files against the upstream v0.2.4 tag and found three planted soundness bugs."

**D3_PROVENANCE: UPSTREAM** — `IntegerTranslation.java` was never read locally before upstream contact; the defect line surfaced only in the diff output (@249267: `oldType` vs `resultType`), the region was read after (@260898), and the edit (@265059) copies the upstream line.

**UPSTREAM_USE: SELECTIVE_BACKPORT** — three targeted Edit-tool hunks backported from the fetched v0.2.4 raws (@94349, @98359), then both files verified byte-for-byte identical to upstream modulo CRLF (@384949 "Bitabs OK / Integer OK") — coverage is functionally whole-file.

**PROTOCOL: CLEAN** — only external contacts are `raw.githubusercontent.com/ultimate-pa/ultimate/v0.2.4` (the repo/tag/files named in the arm C instruction); no issue #1541, terminal-bench, oracle, or hidden-test material appears anywhere in the transcript.

**OVERALL_CLASS: UPSTREAM-LED** — all three defects were located and corrected via the upstream-vs-local diff; no observable local semantic diagnosis preceded upstream exposure (all pre-fetch thinking blocks are redacted/empty, visible text states none). Local work contributed only reproduction and post-hoc discriminating tests.

**STAGES:** D1=VP D2=VP D3=VP

## Chronology

1. @3936–@13747 — explore `/app`, read `bug_demo.c`, reproduce: `Ultimate.py` on `bug_demo.c` returns TRUE (should be FALSE).
2. @19819–@23495 — `wc -l` both suspect files; full Read of local `BitabsTranslation.java` (509 lines). `IntegerTranslation.java` **never read pre-upstream**. No hypothesis stated in visible text; thinking redacted.
3. @92702 — `git status` in ultimate-src fails (not a git repo) → pivots to fetching baseline.
4. @94349 — **first upstream contact**: curl raw v0.2.4 `BitabsTranslation.java`; @98359 curl v0.2.4 `IntegerTranslation.java` + `diff -u` both (80KB output, read @135088); @247890 CRLF-clean diff isolates D3 (`oldType`→`resultType`).
5. @265059 Edit IntegerTranslation (D3); @308784 Edit BitabsTranslation XOR (D2); @346935 Edit BitabsTranslation shift (D1) — all copying upstream text.
6. @384949 — verify both files byte-identical to upstream ("Bitabs OK / Integer OK"); @402357 `javac` 11 compile against plugin JARs; @412595 `jar uf` patches both classes into `cacsl2boogietranslator_0.2.4.jar` (backup kept).
7. @418215–@419843 — `bug_demo` on fixed JAR → FALSE.
8. @433722–@488960 — post-hoc discriminating tests: `xor_test` (D2) TRUE on buggy / FALSE on fixed; `conv_test` (D3, first try) failed to discriminate (FALSE on buggy); `conv_test2` (D3) TRUE on buggy / FALSE on fixed; `safe_test` exercising all three paths → TRUE on fixed (no over-approximation regression).

## Notes

All thinking blocks are redacted (empty string + signature; only token-count estimates survive), so any latent pre-fetch local reasoning is unobservable — per the evidence hierarchy this defaults the provenance to the observable diff-driven path.

The final summary contains substantively correct semantic explanations of all three defects (Nutz lazy-wraparound interaction, `a|b` vs xor identity, wrong modulus), but these are retrospective, written after upstream exposure and all edits, and cannot upgrade provenance.

UPSTREAM_USE sits on the SELECTIVE_BACKPORT / WHOLE_FILE_REPLACEMENT boundary: mechanism was three hunk edits, but the agent explicitly verified and described the result as restoring upstream "byte-for-byte modulo CRLF" in both files.
