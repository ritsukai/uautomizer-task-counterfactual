# O-B03

- **Arm** B (generic upstream-comparison sentence, no coordinates) · **Model** claude-opus-4-6 via Claude Code CLI · **Reward** 0 · **Score** 14/22 (baseline 8/22, delta +6 — the best arm-B result)
- Transcript: `agent/claude-code.txt`, 1,987,395 bytes (longest in the dataset)

> The only trial in which **both** D1 and D2 were diagnosed and repaired by pure local reasoning. It failed because a **decoy** gave it false closure.

## Provenance

**D1_PROVENANCE: LOCAL** — main-agent 14K thinking at 898015 (20:35:07) traces `-1 div 2^28 = -1` under Boogie euclidean semantics and names the constant-shift fast path as unwrapped, ~45s before the Explore subagent's report text (987445) and 3 min before any second opinion; edit at 1021175; zero upstream material exists anywhere in the transcript.

**D2_PROVENANCE: LOCAL** — thinking at 1006348 (excerpt at 1008500) **derives `a+b = (a^b) + 2(a&b)` from scratch** and notes "the code's comment states this formula but actually implements `a+b-(a&b)`, meaning it's computing OR instead of XOR"; edit at 1103250. Neither subagent report mentions XOR (report at 987445 contains zero occurrences of "xor").

**D3_PROVENANCE: NONE-ATTEMPTED** — `IntegerTranslation.java` was never edited (only three source Edits in the whole run: 1021175, 1103250, 1942963); the buggy `applyWraparound(loc, resultType, operand)` sat in context at 567731 **directly beside the correct `applyWraparound(loc, oldType, operand)`** and was never flagged.

**UPSTREAM_USE: SEARCH_NO_BASELINE** — two local `git log`/`git diff HEAD~5` probes on `/app/ultimate-src` (27395 → "Not a git repo"; 57208 → empty) plus a stated intent at 1009067 ("codebase seems derived from an upstream open-source project with deliberate modifications, I should try to trace which parts diverge from the original") — and nothing further: zero WebFetch, zero WebSearch, zero curl/wget/git-clone anywhere in 1.99 MB.

**PROTOCOL: CLEAN** — no occurrence of "1541", "terminal-bench", "harbor" outside the shipped `bug_demo.c` header the agent cat'd at 4131, no oracle/hidden-test/trajectory material, no network egress at all.

**FAILURE_MODE: INCOMPLETE_MULTI_DEFECT** — D1 and D2 were correctly diagnosed, correctly repaired, compiled and deployed into the plugin JAR; D3 was never attempted, and all 8 hidden-test failures (6 "expected TRUE got FALSE" false alarms + the 2 `widen_ushort` cases) are exclusively narrow-unsigned→unsigned-int widening programs, i.e. **D3 alone accounts for 8/22**. Secondary factor: a decoy repair (see notes) gave the agent false three-of-three closure and it stopped searching at 21 min of a 7200s budget.

**STAGES:** D1=VP D2=VP D3=CL

**BANNER: NOT observably noticed**; upstream attempted only as two local git probes; broke down at SEARCH_NO_BASELINE (no baseline ever obtained, no network route ever tried). Evidence: "Version 0e0057cc" appeared in Ultimate stdout at 8391 (first run) and again at 1197872 (post-fix run), but the agent's immediately following text at 11341 and every later text/thinking never mentions the version string, the commit hash, "0e0057cc", "github", or "ultimate-pa" (grep: 4 hits, all inside tool output, 0 in agent-authored text).

## Chronology

1. 8203 (20:29:37) — first Ultimate run returns TRUE where FALSE expected; the same tool result carries `Version 0e0057cc` at 8391, unremarked.
2. 27395 + 57208 (20:29:57–20:30:02) — the trial's only upstream-baseline actions: two `git log` probes → "Not a git repo" / empty; abandoned immediately, never retried by any other route.
3. 561304→567731 (20:30:42) — `grep -rn applyWraparound` output puts **the D3 defect (`IntegerTranslation.java:361`, `resultType`) adjacent to the correct line 370 (`oldType`)** in main-agent context; no reaction.
4. 898015 (20:35:07) — main agent's 14,032-char local diagnosis of the constant-shift path; 1021175 (20:36:05) D1 edit; 1006348 (20:36:02) XOR identity derived; 1103250 (20:37:18) D2 edit.
5. 1184210–1193616 (20:37:59–20:38:11) — `javac` against the plugins classpath + `jar uf` into `…cacsl2boogietranslator_0.2.4.jar`; 1201054 (20:38:28) `bug_demo` now FALSE.
6. 1231921 (20:40:21) — second Explore subagent dispatched to "search for more bugs" across 7 files; its 23K analysis at 1672676 **explicitly reviews `convertToIntegerType`** ("it only applies wraparound if the old type was also unsigned and the new type is wider… otherwise it leaves the operand unchanged") **and pronounces it correct** — the decisive near-miss on D3.
7. 1201916–1899759 (20:38–20:47) — the bulk of the extra runtime: 15 hand-written C probes each run through Ultimate at ~5–20 s, plus manual audits of `constructDivExpression`, `TypeSizes`, `ISOIEC9899TC3`. **Not one probe uses `unsigned short` or `unsigned char`** — the exact blind spot.
8. 1942963 (20:48:59) — third edit reverts `TranslationSettings.isBitvectorTranslation()` to `return mBitvectorTranslation`; recompile+jar at 1963869/1965431; 1982050 (20:50:13) final summary claims "three bugs" fixed; `end_turn` at 21 min wall / 97 turns / $11.61.

## Notes

- The extra runtime went to (a) repeated full-file Reads of six large Java files — why the transcript is 2 MB while the session is only 21 minutes — (b) ~10 minutes of self-authored regression probing after the real fixes already worked, and (c) a second subagent sweep. **It did not time out; it stopped voluntarily at 21 min of 7200 s.**
- Arm B's sentence produced exactly one observable behaviour change: the stated intent at 1009067 and the two `git log` probes, both inside the first 40 seconds. It never escalated to a named repo, a web fetch, or a JAR/source comparison. **The sentence bought a local `git log` and nothing else.**
- **The agent's "third bug" is a decoy**: `isBitvectorTranslation() { return false; }` is not an injected defect but the benchmark's own anti-bypass defense (`tests/patch_bitvector_defense.py` exists to install it). Reverting it did not trip the four guard tests (all 4 PASSED) and did not change behavior (config default is false), but it plausibly caused the harmful epistemic effect: **having "found three bugs" the agent stopped hunting.** Both Explore subagents independently converged on this same decoy and neither surfaced D3.
- D1/D2 repairs are semantically equivalent to the oracle patches — hence 14/22 and the best arm B score, with the entire 8-test deficit traceable to the untouched one-token `resultType`→`oldType` fix.
