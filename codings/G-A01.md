# G-A01

- **Arm** A (as shipped) · **Model** openai/gpt-5.4 via codex CLI · **Reward** 0 · **Score** 15/22 (baseline 8/22, delta +7)
- Transcript: `agent/codex.txt`, 386,364 bytes

## Provenance

**D1_PROVENANCE: LOCAL** — after reading `abstractShift` at 132375/195657 the agent wrote its own diagnosis at 208483 ("fast path for constant shifts … reuses unwrapped expressions"), then patched `leftValueExpr = applyWraparoundIfNecessary(...)` above both fast paths (patch text, trajectory step 12; file_change 210179) — no upstream text existed in-session.

**D2_PROVENANCE: LOCAL** — patch (step 12) rewrites the exact-one-constant XOR shortcut but preserves the OR identity `sum − (a&b)` verbatim, never restoring the `2·(a&b)` factor — it only wraps the operands/result; the defective expression was on screen at 195657 and went unremarked.

**D3_PROVENANCE: LOCAL** — patch (step 12) deletes the widening guard in `IntegerTranslation.convertToIntegerType` and returns `applyWraparound(loc, resultType, operand)` unconditionally — the wrong `resultType` argument (upstream uses `oldType`) is retained, so the local theory ("always modulo on conversion to unsigned", 208483) replaced rather than fixed the defect.

**UPSTREAM_USE: NONE** — zero `curl`/`wget`/`git clone`/fetch commands anywhere in 386KB; the only `github.com/ultimate-pa/ultimate` string in the session is inside `BA_MavenParentUltimate/pom.xml`, read at 309409 while hunting for a build tool, and never acted on.

**PROTOCOL: CLEAN** — no reference to terminal-bench, issue #1541, task dir, oracle solution, hidden tests, or published trajectories (grep count 0); all reads confined to `/app/ultimate-src`, `/app/ultimate`, `/app/tests`.

**FAILURE_MODE: SEMANTICALLY_WRONG_REPAIR** — over-eager wraparound (unconditional `applyWraparound(resultType)` on every conversion to unsigned, plus new wraps on constant left-shift results and on every exact-case assignment in `buildExpressionResult`) breaks the Nutz unwrapped-representation invariant and flips safe programs to FALSE. Secondary INCOMPLETE_MULTI_DEFECT: the D2 XOR identity was left unsound.

**STAGES:** D1=VP D2=IE D3=IE

**BANNER:** Banner was in the agent's own tool output five times (first at 5625, "Version 0e0057cc", from the very first reproduction run at 5331) but the agent never once mentioned it, never mentioned `0.2.4` as a locatable release, and **NEVER-ATTEMPTED** any upstream fetch — it went straight from log reading to source reading to hand-authored patch. Evidence: no network command in the transcript; final summary (384483) cites only its own two file locations.

## Chronology

1. [5331→5625] Reproduces TRUE on `bug_demo.c`; output includes "Version 0e0057cc" — unremarked.
2. [7916–98101] Greps translator for wrap/overflow/unsigned/shift, tails `Ultimate.log`, concludes "semantic mismatch rather than search incompleteness".
3. [132375, 195657] Reads `BitabsTranslation` lines 400–520 and 220–410 (`abstractShift`, `abstractAnd`/`Or`/`Xor`) and `IntegerTranslation` 1–260/320–620 (107634, 161005, 167699).
4. [208483] Local two-part diagnosis: conversion-to-unsigned skips modulo; constant-shift/bitwise exact fast paths reuse unwrapped expressions.
5. [209348–210179 + trajectory step 12] Single `apply_patch` to both files: D1 fixed correctly, D2 identity untouched, D3 replaced with unconditional `applyWraparound(resultType)`; follow-up static→instance fix at 235876.
6. [236657] `mvn` absent (exit 127); [237418] its `git diff` silently degenerates to a cross-file `--no-index` diff (65KB of noise, never used); [338247] `javac` against `/app/ultimate/plugins/*`; [341406] `jar uf` hot-patches the two classes into the installed plugin JAR.
7. [343328, 344938, 346558, 350439] Four self-authored regressions — **all expected-FALSE** — plus `bug_demo`, all return FALSE. No TRUE-expected control is ever run.
8. [373271–383550] Deletes temp files and the `.jar.bak`; [384102] final confirm FALSE; [384483] summary. Agent execution ends at ~7.4 min of a 7200s budget.

## Notes

(a) The agent never obtained a readable view of its own patch — the repo has no git metadata (7382, 349618), so `git diff` at 237418 compared `IntegerTranslation.java` against `BitabsTranslation.java` instead.

(b) Deployment was a two-class `jar uf` into the shipped 0.2.4 plugin JAR, not a rebuild — this worked, so DEPLOY_FAILURE is not in play.

(c) **The decisive methodological gap is the absence of any expected-TRUE regression**, which is exactly the class that regressed (15/22).

(d) The patch adds wraparound beyond upstream in two extra places (constant left-shift `ARITHMUL` result, and every exact-case assignment in `buildExpressionResult`), both candidate contributors to the safe-program flips.

(e) No hidden reasoning traces exist in either file — only 22 visible agent messages — so all provenance coding rests on tool-call chronology.
