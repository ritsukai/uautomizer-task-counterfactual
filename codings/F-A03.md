# F-A03

- **Arm** A (as shipped) · **Model** claude-fable-5 (xhigh) · **Reward** 1 · **Score** 22/22
- Transcript: `agent/claude-code.txt`, 651,640 bytes

## Provenance

**D1_PROVENANCE: UPSTREAM** — exact upstream shift-hunk (`leftWrapped` moved before constant fast path) first seen in diff result @~339575 (L152); Edit @465318 applies it verbatim; no observable local diagnosis of D1 before that (all thinking blocks redacted, no TEXT hypothesis).

**D2_PROVENANCE: UPSTREAM** — upstream XOR hunk (`2 * (a & b)` multiplied term) visible in same diff @~339575; Edit @501374 is a verbatim backport; only prior local activity is a full file Read @149335 with no stated hypothesis.

**D3_PROVENANCE: UPSTREAM** — `applyWraparound(loc, oldType, …)` vs `resultType` hunk legible in whitespace-insensitive diff @~365419 (L158); Edit @541133 applies it verbatim; IntegerTranslation had been Read @56613 but no D3 hypothesis appears before the diff.

**UPSTREAM_USE: SELECTIVE_BACKPORT** — agent self-identified commit `0e0057cc` from the binary's version banner (@13387), confirmed via api.github.com @318931, sparse-cloned ultimate-pa/ultimate @329877, diffed the plugin @332265, backported exactly the 3 semantic hunks (leaving the 4th inert `isBitvectorTranslation()→false` diff untouched with reasoned justification), then verified byte-exact upstream match @586818.

**PROTOCOL: CLEAN** — zero hits for issue #1541 / terminal-bench / oracle / solution / hidden-test / trajectory strings; only "harbor-canary" GUID inside the task-provided `bug_demo.c` itself; sole external contact is genuine upstream github.com.

**OVERALL_CLASS: UPSTREAM-LED** — the declared strategy was "diff the entire shipped source against upstream at that exact commit to find every injected change" (@324901); the diff both located and corrected all three defects, and all edits are verified-verbatim upstream backports. Local work contributed reproduction, repo/commit identification, and post-diff mode analysis — not any defect's semantic fix.

**STAGES:** D1=VP D2=VP D3=VP

## Chronology

1. @11726–19445 — reproduce: `Ultimate.py` on `bug_demo.c` yields TRUE (should be FALSE); version string `0e0057cc` captured in output.
2. @38541–307733 — local reads: grep wraparound; full Reads of `IntegerTranslation.java` (@56613), `BitabsTranslation.java` (@149335), `ExpressionTranslation.java` (@245030, "base class looks fine" @309495); no defect hypothesis stated in any observable channel.
3. @311432/@318931 — first upstream contact: connectivity probe to github.com, then `api.github.com/repos/ultimate-pa/ultimate/commits/0e0057cc`.
4. @329877–333272 — blob-filtered clone + sparse checkout of CACSL2BoogieTranslator at `0e0057cc`; `diff -r --brief` finds exactly 3 differing files (+ locally-deleted `BitvectorTranslation.java`).
5. @338375–365419 — full diffs expose upstream fix text for D1+D2 (@~339575) and D3+TranslationSettings (@~365419).
6. @391597–444887 — local analysis: bitvector translation disabled (no `*_Bitvector.epf`, preference default false) → TranslationSettings diff judged inert scaffolding, not backported.
7. @465318 (D1), @501374 (D2), @541133 (D3) — three verbatim backport Edits; @586818 → "BITABS_MATCHES_UPSTREAM | INTEGER_MATCHES_UPSTREAM".
8. @588684 `javac` against deployed plugin JARs; @594193 `jar uf` patch of `cacsl2boogietranslator_0.2.4.jar` (backup kept); @599736→@601325 `bug_demo` now FALSE; @615731–@640574 five self-authored differential tests (lshift_false/shift_true=D1, xor_false/xor_true=D2, conv_false=D3) all correct.

## Notes

All thinking blocks in this transcript are redacted (empty `thinking` field, signature only) — ~20KB apparent gaps are signature bytes, so any private diagnosis is unobservable and cannot be credited per the evidence hierarchy.

The agent bridged Arm A's missing repo name entirely on its own via the tool's version banner. Final summary (@643981) gives fluent semantic explanations (Nutz transformation, congruence mod 2^32) but these are retrospective, post-upstream-exposure.

Anomaly worth noting: the task image contained a **fourth** divergence from upstream (`TranslationSettings.isBitvectorTranslation` hardcoded to false + deleted `BitvectorTranslation.java`), which the agent detected and deliberately left in place.
